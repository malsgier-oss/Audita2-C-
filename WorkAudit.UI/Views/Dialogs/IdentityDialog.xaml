<Window x:Class="WorkAudit.UI.Views.Dialogs.IdentityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Identity"
        Width="400"
        Height="220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Please enter your identity to continue." Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Name:" Width="80" VerticalAlignment="Center" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="250" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Staff ID:" Width="80" VerticalAlignment="Center" />
            <TextBox Text="{Binding StaffId, UpdateSourceTrigger=PropertyChanged}" Width="250" />
        </StackPanel>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Command="{Binding OkCommand}" IsDefault="True" Width="80" Margin="0,0,8,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" Width="80" />
        </StackPanel>
    </Grid>
</Window>
